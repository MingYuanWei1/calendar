# 学生端浏览器验收

测试边界：真实浏览器中的可见行为，使用内置浏览器操作，不调用应用内部函数。

1. 390×844 打开学生端，搜索“艺术”，只显示校园艺术节；切换英文后搜索条件仍在；关闭详情后搜索与列表滚动位置保持。
2. 电脑默认 19 项；选初中部为 13 项，排除活动为 7 项。重置后恢复 19 项；9 月 23 日展开 6 项。
3. 切换月份与日程视图不清除筛选。没有结果有提示和重置，截止时刻与取消状态有文字。
4. 使用独立扩展示例检查跨周与重叠跨日条，单日可见条目不超过 4；隐藏部分可通过当天列表全部访问。

首次红灯：手机标题搜索被 CSS 隐藏，无法执行场景 1。

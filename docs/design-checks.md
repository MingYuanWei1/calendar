# 方向稿检查记录

检查日期：2026-09-19。检查对象为静态设计稿，不代表后台或生产应用已实现。

## 已检查

- JavaScript 语法检查通过。
- 在 Codex 内置浏览器实际打开三套方案，检查 Logo、月历、详情与布局差异。
- 同月数据共 19 项；筛选初中部后为 13 项，含全校事件。
- 不匹配的搜索词显示空结果与重置入口。
- 9 月 23 日的“还有 2 项”展开后包含当日全部 6 项事件，其中一项为跨日阅读周。
- 切换英文后，详情名称、时间、地点、适用范围、主办方与说明均显示英文。
- 报名按钮打开示意说明，不提交数据，不跳到虚构报名地址。
- 运动会详情显示原定 9 月 20 日、现定 9 月 22 日；音乐会显示取消状态并保留记录。
- 390 × 844 手机尺寸下检查日程列表、详情与关闭返回；手机详情限制背景操作并支持键盘退出。

## 检查中修正

- 跨日事件按起始日期分配行，互不重叠的阅读周与校假共用同一行，避免无意义的额外留白。
- 月份切换同步更新左侧小月历。
- 关闭手机详情后，将键盘焦点返回到可见的列表事件。

## 当前边界

首轮只比较视觉方向。事件均为虚构数据，今天固定为 2026-09-19。后台、持久化、真实链接、海报与二维码的完整状态在选定方向后继续设计。手机首轮提供学部／类型筛选及列表详情，手机搜索布局留待选定方向后的完整页面设计。

## C＋侧栏修订检查

- 评审页默认展示已选 C 方案，左栏包含学部与事件类型复选框。
- 浏览器验证：初中部筛选为 13 项，进一步取消“活动”类型后为 7 项，组合筛选正常。
- 较窄电脑窗口初始隐藏详情，点击事件后侧边展开，保留月历宽度。手机继续使用原有列表筛选布局。
